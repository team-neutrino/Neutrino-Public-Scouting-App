let array = [
  "score fuel",
  "action1"
]

function addAction(numbe) {
  compressList.push(number);
  
  let string = "";
  for (let action in compressList) {
    string += array[action] + "\n"
  }

  document.getElementById('terminal').innerHTML = string;
}
